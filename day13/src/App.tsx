// Create a todo web app where we can add task name, description, tag, status, time, priority and on top it should show progress of the task use local storage for maintainence

import TodoPage from "./pages/TodoPage"

const App = () => {
  return (
    <div>
      <TodoPage />
      
      
    </div>
  )
}

export default App